# 01 — `command-run` (core)

**Status:** validated  
**Real-data source:** local

Builtin: cilock always runs this. Captures argv, environment, exit code, and (with --trace) ptrace-level syscall info.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
