# 10 — `maven` (build)

**Status:** validated  
**Real-data source:** local

Real pom.xml resolution: groupId, artifactId, version, dependencies extracted statically.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
