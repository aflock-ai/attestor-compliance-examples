# 05 — `git` (core)

**Status:** validated  
**Real-data source:** local

Real .git/ discovery: HEAD commit, branch, tag, parent, author, signature.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
