# 03 — `material` (core)

**Status:** validated  
**Real-data source:** local

Builtin: hashes files present in workingdir before the wrapped command runs.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
