# 02 — `product` (core)

**Status:** validated  
**Real-data source:** local

Builtin: classifies files written during the wrapped command and emits per-file sha256 digests.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
