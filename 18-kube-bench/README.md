# 18 — `kube-bench` (container)

**Status:** pending  
**Real-data source:** vm-eks

Real `kube-bench --benchmark eks-1.5.0 --json` Job deployed against `dropbox-clone-dev` EKS cluster.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
