# 06 — `configuration` (core)

**Status:** validated  
**Real-data source:** local

Real .witness.yaml / cilock config capture (when present in workingdir).


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
