# 23 — `jenkins` (ci)

**Status:** validated  
**Real-data source:** local-env

Validated by setting canonical Jenkins env vars in shell — attestor reads from env, no other inputs.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
