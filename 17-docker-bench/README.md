# 17 — `docker-bench` (container)

**Status:** pending  
**Real-data source:** vm

Real `docker-bench-security.sh -j` scan against the validation VM's Docker daemon.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
