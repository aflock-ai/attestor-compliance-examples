# 31 — `steampipe` (compliance)

**Status:** pending  
**Real-data source:** vm

Real `steampipe query 'select arn, name from aws_iam_user limit 5'` against testifysec-demo.


## Reproduce

See [`reproduce.sh`](./reproduce.sh) for the exact validated invocation.
